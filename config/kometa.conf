CONFIG_ZMK_PM_SOFT_OFF=y

# Keymap mirrors corne.keymap (no &soft_off binding anymore, so the soft-off
# behavior above is reachable only if a future keymap binds it again).
#
# ZMK Studio is enabled on the left/central half via build.yaml
# (unlock via ADJUST = hold LOWER + RAISE, third key of the right top row)
